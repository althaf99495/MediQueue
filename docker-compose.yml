services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - REDIS_URL=redis://redis:6379/0
      - FLASK_ENV=production
      - SESSION_SECRET=dev-secret-key-change-in-production
      - DATABASE_URL=sqlite:////app/instance/mediqueue.db
    volumes:
      - ./instance:/app/instance
      - ./static/uploads:/app/static/uploads
    depends_on:
      - redis
    restart: always

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:


